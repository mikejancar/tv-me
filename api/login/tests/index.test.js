const assert = require('assert');
const events = require('./example-events.json');
const series = require('../index');

describe('TV Me API - Login Resource - Unit Tests', function () {
  describe('handler', function () {
    // NOTE: Need to figure out how to run tests using AWS services
    // it('should return a successful response when called with a valid user to add', async function () {
    //   const response = await series.handler(events.postAddUser);
    //   assert.equal(response.statusCode, 200);
    //   assert.ok(response.body);
    // });
  });
});
